import React, { useEffect, useState } from 'react'
import axios from

const Row = ({title, fetchUrl, isLargeRow}) => {
    const [movies, setMovies] = useState([]);
    useEffect(() =>{
        const fetchData = async () => {
            // const response = await axios.
            // yarn add axios: START HERE
        }
    })
    return (
        <div>
            
        </div>
    );
};

export default Row;
